# Database Status

No database, ORM, schema, or migration system is configured.

The SDK check uses SessionManager.inMemory(cwd). It does not demonstrate persistence. Trellis development tasks and journals are files managed by Trellis.

Choose product persistence from a real task's requirements; do not treat conversational memory as authoritative acceptance, deployment, or business data.
